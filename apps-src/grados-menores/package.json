{
  "name": "grados-menores-webapp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5177",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5177",
    "check": "npm run build",
    "deploy": "npm run build && node scripts/copy-dist.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.13"
  }
}
