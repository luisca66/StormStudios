{
  "name": "desglose-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Desglose — entrenamiento auditivo (canta cada nota de un acorde). UI Vite+TS; se compila a public/apps/desglose/.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/copy-dist.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
